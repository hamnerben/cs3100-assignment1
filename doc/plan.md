## `main.java`

```
// check for good input
goodInput = false
if args.lenth is even:   
    good input = True
    for arg
        if sanity check on arg fails
        good input = False

if not goodInput:
    print error message

// main proccess
for arg tag // iterate by 2 not one so it skips the numbers
    if(tag == fib)
        fib(arg + 1)
    elif(tag == fac)
        fac(arg + 1)
    elif(tag == e)
        e(arg + 1)
        
        
// fibonacci 
fib(int n)
    if( n is in range)
    
    else:
        print range

// factorial
fac(int n)
    if( n is in range)
    
    else:
        print range

// e
e(int n)
    if( n is in range)
    
    else:
        print range

```